name: Community bounded crawl

on:
  schedule:
    - cron: '7,22,37,52 * * * *'
  workflow_dispatch:
    inputs:
      source:
        description: 'Source to crawl (scheduled runs remain Inven until Bobaedream canary is enabled)'
        required: false
        default: 'inven'
        type: choice
        options:
          - 'inven'
          - 'bobaedream'
          - 'all'
      pages:
        description: 'Number of recent Inven pages (1-3)'
        required: false
        default: '3'
        type: choice
        options:
          - '1'
          - '2'
          - '3'

concurrency:
  group: community-crawl
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      YAKHU_SITE_URL: ${{ secrets.YAKHU_SITE_URL }}
      YAKHU_INGEST_SECRET: ${{ secrets.YAKHU_INGEST_SECRET }}
      CRAWL_SOURCE: ${{ inputs.source || 'inven' }}
      CRAWL_PAGES: ${{ inputs.pages || '3' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Install
        run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Crawl and ingest
        run: npm run crawl
